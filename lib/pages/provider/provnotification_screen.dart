import 'package:flutter/material.dart';
import 'package:agriser/bloc/provider/drawer_bloc.dart';

class ProvNotificationScreen extends StatelessWidget with DrawerStates {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class ProvEditNotificationScreen extends StatelessWidget with DrawerStates {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
